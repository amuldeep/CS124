/*#include "Telegraph.h"
std::string Telegraph::decode(std::string morse){
    string msg = "";
    for (each cahr in morse){
        Tnode* node;
        if (morse[i] == DOT){
            node = node->left;
        }
        else if(morse[i] == DASH){
            node = node->right;
        }
        else{
            }//space
        }
    msq += node->symbol;
    node = root;
}
        
*/
