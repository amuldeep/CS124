/*#include "Telegraph.h"
std::string Telegraph::encode(std::string text)
{
    std::string morse = "";
    char c;
    for(int i = 0; text[i]; i++){
        c = toupper(text[i])
        if (c==' '){
            morse += " "; continue;}
        }
        int j;
    for(j=0;table[j].symbol; j++){
        if(table[j].symbol == c){
            break;}
        morse += table[j].code;
        morse += " ";
    }
    return morse;
}
*/
